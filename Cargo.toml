[workspace]
members = [
        "breakpad-symbols",
        "minidump",
        "minidump-common",
        "minidump-processor",
        "minidump-stackwalk",
        "minidump-synth",
]
exclude = [
    "testdata/*",
]

[workspace.metadata.release]
shared-version = true
consolidate-commits = true
consolidate-pushes = true
tag-name = "v{{version}}"
